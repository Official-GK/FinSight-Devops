# ──────────────────────────────────────────────────────────────────────────────
# Analytics API — Test-only dependencies
# Install alongside requirements.txt: pip install -r requirements-test.txt
# ──────────────────────────────────────────────────────────────────────────────

# Core test framework
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# HTTP client for async FastAPI testing
httpx==0.27.0

# Mocking
pytest-mock==3.14.0

# For freezing time in retry backoff tests
freezegun==1.5.0

# Code quality (also run in Jenkins Lint stage)
ruff==0.4.4
mypy==1.10.0

# Optional: structured test output for SonarQube
pytest-json-report==1.5.0
